<div class="container m-6">
        <div class="columns">
            <div class="column">
                <h1 class="title">Servicio web</h1>
                <div class="card mt-6">
                    <div class="card-content">
                        <div class="content">
                            
                            <table class="table">
                                <thead>
                                    <tr>
                                        <th>Title</th>
                                        <th>Description</th>
                                        <th>Action</th>
                                    </tr>
                                </thead>
                                <tbody>
                                {{#each todos}}
                                    <tr>
                                        <td>{{this.title}}</td>
                                        <td>{{this.description}}</td>
                                        <td><a href="/todo-delete/{{this.id}}">Delete</a>
                                    </tr>
                                {{/each}}
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>